#ifndef TYPES_H
#define TYPES_H

#include <vector>
#include <string>

using message_t = const std::string&;
using messages_t = const std::vector<std::string>&;
using errors_t = std::vector<std::string>;

#endif // TYPES_H
